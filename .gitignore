# local type-check harness — machine-specific paths into the pi install
tsconfig.pi.json

# dev dependencies installed only for local type-checking
node_modules/

# this package ships no npm dependencies
package-lock.json
